package kr.hee.kwnoti.info_activity;

import java.util.ArrayList;

public interface DataReceived {
    /**
     * Make list view using array data
     * @param arr the data that received by parser
     */
    void makeView(ArrayList arr);
}
